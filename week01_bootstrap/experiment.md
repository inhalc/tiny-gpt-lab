# experiment

## 运行 1

命令：

```bash
python week01_bootstrap/train.py
```

- `lr`:
- 开始几步的 `loss`:
- 最后几步的 `loss`:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 运行 2

命令：

```bash
python week01_bootstrap/train.py
```

- `lr`:
- 开始几步的 `loss`:
- 最后几步的 `loss`:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 观察

-
