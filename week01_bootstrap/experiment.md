# experiment

## 运行 1

命令：

```bash
python week01_bootstrap/train.py
```

- `lr`:
- `steps`:
- `noise`:
- `batch_size`:
- `device`:
- 开始几步的 `train_loss / eval_loss`:
- 最后几步的 `train_loss / eval_loss`:
- 最后的 `w / b`:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 运行 2

命令：

```bash
python week01_bootstrap/train.py
```

- `lr`:
- `steps`:
- `noise`:
- `batch_size`:
- `device`:
- 开始几步的 `train_loss / eval_loss`:
- 最后几步的 `train_loss / eval_loss`:
- 最后的 `w / b`:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 观察

-
