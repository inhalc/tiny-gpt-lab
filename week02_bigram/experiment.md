# experiment

## 运行 1

命令：

```bash
make train CONFIG=dev STAGE=bigram
```

- `lr`:
- `max_steps`:
- `batch_size`:
- `device`:
- 开始几步的 `train_loss / val_loss`:
- 最后几步的 `train_loss / val_loss`:
- 采样片段（如有）:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 运行 2

命令：

```bash
make train CONFIG=dev STAGE=bigram
```

- `lr`:
- `max_steps`:
- `batch_size`:
- `device`:
- 开始几步的 `train_loss / val_loss`:
- 最后几步的 `train_loss / val_loss`:
- 采样片段（如有）:
- 变化：下降 / 震荡 / 发散 / 不确定
- 备注：

## 观察

-
