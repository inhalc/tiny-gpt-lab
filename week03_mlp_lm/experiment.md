# experiment

## 运行 1

命令：

```bash
make train CONFIG=dev STAGE=mlp
```

- `block_size`:
- `lr`:
- `max_steps`:
- `device`:
- week02 最佳 val_loss（对比用）:
- 本次 val_loss:
- 备注：

## 运行 2

命令：

```bash
make train CONFIG=dev STAGE=mlp
```

- 改动项:
- val_loss:
- 备注：

## 观察

-
