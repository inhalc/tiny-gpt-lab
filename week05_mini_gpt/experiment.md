# experiment

## 运行 1

命令：

```bash
make train CONFIG=dev STAGE=gpt
```

- `n_layer` / `n_head` / `n_embd`:
- `max_steps`:
- `device`:
- 参数量（日志里有）:
- 最后 train_loss / val_loss:
- 备注：

## 采样

命令：

```bash
make sample CONFIG=dev PROMPT="ROMEO:"
```

- 生成片段（贴一小段）:
- 备注：

## 观察

-
