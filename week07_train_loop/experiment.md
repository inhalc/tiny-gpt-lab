# experiment

## 运行 1

命令：

```bash
make train CONFIG=dev STAGE=gpt2
```

- `lr`:
- `max_steps`:
- `device`:
- checkpoint 路径:
- 末步 train_loss / val_loss:
- 备注：

## 运行 2

命令：

```bash
make train CONFIG=dev STAGE=gpt2
```

- 改动项:
- 末步 train_loss / val_loss:
- 备注：

## 观察

-
